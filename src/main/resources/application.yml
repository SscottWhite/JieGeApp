spring:
## 1.1
  profiles:    active:    - dev
    - production
    - prod
## 1.2
  mvc:    view:      prefix: /WEB-INF/jsp/
      suffix: .jsp
## 1.3
  datasource:
    # 自定义数据源
    type: com.alibaba.druid.pool.DruidDataSource 
    #导入springboot-druid
    druid:
     username: ncstudy
     password: wiska123
     url: jdbc:mysql://47.102.155.32:3306/JieGeApp?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
     driver-class-name: com.mysql.jdbc.Driver
     #Spring Boot 默认是不注入这些属性值的，需要自己绑定
     #druid 数据源专有配置
     stat-view-servlet: 
       login-username: admin
       login-password: 123456
     initialSize: 5
     minIdle: 5
     maxActive: 20
     maxWait: 60000
     timeBetweenEvictionRunsMillis: 60000
     minEvictableIdleTimeMillis: 300000
     # validationQuery: SELECT 1 FROM DUAL     #oracle
     validation-query: SELECT 'x'   #mysql
     testWhileIdle: true
     testOnBorrow: false
     testOnReturn: false
     poolPreparedStatements: true
     #配置监控统计拦截的filters，stat:监控统计、log4j：日志记录、wall：防御sql注入
     #如果允许时报错  java.lang.ClassNotFoundException: org.apache.log4j.Priority
     #则导入 log4j 依赖即可，Maven 地址：https://mvnrepository.com/artifact/log4j/log4j
     filters: stat,wall,log4j
     maxPoolPreparedStatementPerConnectionSize: 20
     useGlobalDataSourceStat: true
     connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=500

## 2.1
logging:
  config: classpath:logback-spring.xml
  level: 
    #指定包名或者类名下的日志输出级别
    com.ncstudy: trace
## 日志级别从低到高分为：TRACE < DEBUG < INFO < WARN < ERROR < FATAL 。如果设置为 WARN ，则低于 WARN 的信息都不会输出。
# private final static Logger logger = LoggerFactory.getLogger(xx.class);  


## 3.1
mybatis: 
  mapper-locations: 
    - classpath:mappers/*.xml